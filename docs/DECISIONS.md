# Decision Log

The governing principle: **these are not preferences — they are decisions made for specific reasons, and changing them requires going back to those reasons.**

| N | YYYY-MM-DD | Who | Decision |
|---|------------|-----|----------|
| 1 | 2026-08-15 | User & Antigravity | **100% client-side privacy model.** All PDF parsing, preview rendering, and document export run entirely within the user's browser memory. No backend API, Firebase Storage, Functions, or cloud processing. Proof: Zero outbound network requests for document data. Files affected: Entire project architecture. |
| 2 | 2026-08-15 | User & Antigravity | **Strict separation of Preview (pdfjs-dist) and Export (@cantoo/pdf-lib).** Preview rendering produces `<canvas>` pixels solely for UI thumbnails. Export reconstructs the PDF using pdf-lib's `copyPages()` directly from pristine original bytes, preserving vector text, 300+ DPI scans, and embedded font fidelity without rasterization. Proof: Verified export structure contains native vector/PDF objects rather than raster image pages. Files affected: `src/managers/PdfSourceManager.ts`, `src/managers/ThumbnailRenderManager.ts`, `src/managers/PdfExportManager.ts`. |
| 3 | 2026-08-15 | User & Antigravity | **Descriptor-based document state model (`PageDescriptor[]`).** Working document is an ordered array of lightweight descriptors (`id`, `sourceId`, `sourcePageIndex`, `width`, `height`), decoupled from rendered canvases and binary blobs. Allows future non-destructive operations (reorder, rotate, delete) without modifying original source bytes. Proof: Document state mutations manipulate metadata only. Files affected: `src/domain/types.ts`, `src/coordinator/PdfCoordinator.ts`. |
| 4 | 2026-08-15 | User & Antigravity | **Pristine buffer isolation for pdf.js.** `pdfjs-dist` is passed a cloned slice (`originalBytes.slice()`) to prevent web worker transfer/detachment from mutating the original `Uint8Array` needed by `@cantoo/pdf-lib`. Proof: pdf-lib document load succeeds consistently even after pdf.js rendering. Files affected: `src/managers/PdfSourceManager.ts`. |
| 5 | 2026-08-15 | User & Antigravity | **Non-destructive metadata-based page rotation.** Page rotation (0°, 90°, 180°, 270°) is stored in `PageDescriptor.rotation` and rendered in UI via CSS transforms/preview rendering. On PDF export, it is applied directly via `copiedPage.setRotation(degrees(...))` in `@cantoo/pdf-lib`, preserving original vector text and image clarity. Files affected: `src/domain/types.ts`, `src/managers/PdfExportManager.ts`, `src/components/PageCard.tsx`. |
| 7 | 2026-08-15 | User & Antigravity | **Client-side In-App Modal with Direct Browser Download for Image Export.** Image export presents an in-app web modal allowing customization of Base File Name (`ExportedPage`), type format selection (`PNG (*.png)` vs `JPG (*.jpg)`), and page scope (Selected vs All), then downloads rendered high-resolution image blobs directly to the browser without displaying OS file/folder pickers. Proof: Pre-rendered blobs download sequentially with zero native OS prompt interruptions. Files affected: `src/components/ExportImagesModal.tsx`, `src/coordinator/PdfCoordinator.ts`, `src/App.tsx`. |
| 8 | 2026-08-15 | User & Antigravity | **Pointer-Driven Page Lifting, Spring Sliding & Visual (Row, Column) Grid Mapping.** Replaces native HTML5 drag-and-drop with pointer event tracking. When grabbing a card, it lifts into a floating preview while surrounding cards slide smoothly using `framer-motion` spring physics without jumping. Target slot index maps directly to visual $(R \times C_{count} + C)$ grid cell coordinates based on the lifted card's geometric center. Proof: Tested multi-row, single and multi-page dragging on localhost. Files affected: `src/components/PageGrid.tsx`. |
| 9 | 2026-08-15 | User & Antigravity | **Fixed PDF Source Files Sidebar Layout.** Locked viewport layout to `h-screen overflow-hidden` with stationary left sidebar and independent vertical scrolling on the files list and main grid container. Proof: Viewport and autoscroll interactions scroll page content without shifting the sidebar. Files affected: `src/App.tsx`, `src/components/SidebarSources.tsx`. |
| 10 | 2026-08-15 | User & Antigravity | **Universal Closed-Hand Grabbing Cursor Override & Default Button Arrow.** During dragging, a global stylesheet override ensures the closed-hand cursor (`grabbing`) persists across all page elements. Normal button hover uses standard arrow cursor (`cursor: default`) instead of pointing finger. Proof: Tested in browser during drag and button hover. Files affected: `src/components/PageGrid.tsx`, `src/index.css`. |
| 11 | 2026-08-23 | User & Antigravity | **Local Offline Bundling of cMaps & Standard Fonts.** Bundled local binary character maps (`public/cmaps/`) and standard fallback fonts (`public/standard_fonts/`) to guarantee 100% offline, privacy-first execution with zero external network requests to CDNs. Proof: Tested network tab in browser with 0 external network requests. Files affected: `public/cmaps/`, `public/standard_fonts/`, `src/managers/PdfSourceManager.ts`. |
| 12 | 2026-08-23 | User & Antigravity | **Intrinsic Signature Anchoring & Relative Rotation Model.** Anchored signatures to intrinsic page coordinates with `placedRotation`. Signatures placed on rotated pages render upright to the viewer, and subsequent page rotations rotate the signature together with the document across UI, PDF vector export, high-res image export, and print. Proof: `npm run build` code 0, verified across `SignModal`, `PageCard`, `PageGrid`, `PdfExportManager`, and `ThumbnailRenderManager`. Files affected: `src/domain/types.ts`, `src/lib/signatureUtils.ts`, `src/components/SignModal.tsx`, `src/components/PageCard.tsx`, `src/components/PageGrid.tsx`, `src/coordinator/PdfCoordinator.ts`, `src/managers/PdfExportManager.ts`, `src/managers/ThumbnailRenderManager.ts`. |
| 13 | 2026-08-24 | User & Antigravity | **Mobile Responsive Navigation, Slide-Over Drawer & Floating Bottom Action Bar.** Optimized small screens (< 1024px) by collapsing top navbar options into a dropdown "More Options" menu, presenting PDF source files in an on-demand slide-over drawer (`MobileSourcesDrawer.tsx`), and providing a sticky bottom action bar (`MobileBottomBar.tsx`) for thumb controls. Kept desktop view (`lg:` 1024px+) 100% identical and unchanged. Proof: `npx tsc --noEmit` and `npm run build` passed with exit code 0. Files affected: `src/components/TopNavbar.tsx`, `src/components/SidebarSources.tsx`, `src/components/MobileSourcesDrawer.tsx`, `src/components/MobileBottomBar.tsx`, `src/components/GridSubHeader.tsx`, `src/components/PageGrid.tsx`, `src/App.tsx`. |
| 14 | 2026-08-24 | User & Antigravity | **Mobile Dedicated Touch Drag Handles, Pointer Capture & Responsive Modal Footers.** Resolved touch scrolling vs drag reordering conflicts by setting `touch-pan-y` on page card bodies while attaching `touch-none` and `onPointerDown` to dedicated 6-dot `GripVertical` handles. Converted signature placement box drawing (`SignModal.tsx`) to `PointerEvents` with `setPointerCapture`. Constrained `SplitModal.tsx` and `ExportImagesModal.tsx` to `max-h-[92vh]` flex-col containers with scrollable body sections and fixed sticky bottom footers so action buttons never push off mobile screens. Proof: Verified on mobile device; `npm run build` exit code 0, deployed to Firebase. Files affected: `src/components/PageCard.tsx`, `src/components/PageGrid.tsx`, `src/components/SignModal.tsx`, `src/components/SplitModal.tsx`, `src/components/ExportImagesModal.tsx`. |
| 15 | 2026-08-25 | User & Antigravity | **Full-Screen Page Editor, Geometric Visual Text Highlighting & Live Annotation Compositing.** Replaced simple signing overlay with full-screen `PageEditorModal.tsx`. Text highlighting locks into geometric line calculation when starting on text (solid lines, zero word gaps, yellow multiply blend) and smooth quadratic splines in freehand. AcroForm fields support smart auto-fit capped at Medium with instant multiline (<kbd>Enter</kbd>) support. Annotations composite live into page card thumbnails and PDF export. Proof: `npx tsc --noEmit` and `npm run build` passed with exit code 0. Files affected: `src/components/PageEditorModal.tsx`, `src/components/PageCard.tsx`, `src/managers/ThumbnailRenderManager.ts`, `src/managers/PdfExportManager.ts`, `src/managers/PdfSourceManager.ts`, `src/coordinator/PdfCoordinator.ts`, `src/hooks/useThumbnail.ts`. |

## Open Questions
*(None currently deferred)*


